getProfile:
  handler: CRUD/profile/main.main
  events: 
    - http:
        path: /profile/{id}
        method: get
        cors: true
        request:
          parameters:
            paths: 
              id: true #reuqired 
